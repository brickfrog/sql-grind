{
  "challengeId": "reconcile.04",
  "skillId": "reconcile",
  "displayNumber": "reconcile.04",
  "version": "reconcile.04-v1",
  "bundleVersion": "reconcile.04-bundle-v1",
  "assessmentVersion": "exact-v1",
  "title": "Resolve confidence and coverage",
  "brief": "Separate confident decisions, ambiguity, and missing evidence before reporting coverage.",
  "instructions": [
    "Normalize text with lower/trim, replace runs of non-Unicode-letter/digit characters with one space, and collapse whitespace. Do not transliterate Unicode. Account keys use lower/trim with blanks NULL; source keys use trim only with blanks NULL. Keep the smallest partner ID for each non-NULL source key; keep missing keys separate. Map hw, hard ware, and hardware to hardware; supplies remains supplies. Parse strict ISO YYYY-MM-DD, US MM/DD/YYYY, or DD Mon YYYY, in that order; invalid dates become NULL. Preserve all source rows unchanged.",
    "Candidates share a non-NULL normalized account key, even with malformed dates or weak evidence. Description edit distance <=2 earns 30, <=5 earns 15, otherwise 0; missing/empty earns 0. Dates equal/one day/two or three days apart earn 20/15/10, otherwise 0. Equal quantity earns 30, difference one earns 20, otherwise 0. Equal nonempty normalized category earns 10. At least two distinct shared normalized words earns 10. Sum the integer contributions; this is evidence, not probability.",
    "Include singleton groups and one-to-many/many-to-one groups with at most three members per side, never multiple members on both sides. Members share an account block. Multi-member groups require equal aggregate quantity, worth 30 points; other contributions are the minimum constituent pair contributions. Deduplicate identical groups. Canonical keys are r:<numerically sorted comma-separated reference IDs>|p:<numerically sorted comma-separated partner IDs>.",
    "For each reference sort distinct groups by score DESC then key ASC. Margin is the best score minus the second different group score (zero if absent). Below 75 is missing; score >=75 with margin <10 is ambiguous; otherwise >=90 is high and 75..89 probable. Detect overlaps simultaneously among all provisional high/probable groups: different groups sharing any reference or partner make every affected reference ambiguous. Valid many-to-one membership is one group, not a conflict. Return every reference; only high/probable retains a group key. Keep best score/margin after ambiguity; no candidates means zero."
  ],
  "datasetId": "reconciliation",
  "starterSql": "SELECT ref_id, account_key, category, event_date, description, quantity, business_unit\nFROM reference_events\nORDER BY ref_id\nLIMIT 10;",
  "starterExplanation": "Inspect immutable reference identities and raw evidence first. This source-only preview intentionally does not yet resolve competing groups or classify missing evidence.",
  "hints": [
    {
      "level": 1,
      "kind": "concept",
      "text": "Separate winner selection from simultaneous overlap detection."
    },
    {
      "level": 2,
      "kind": "structure",
      "text": "Rank distinct groups per reference, compute the runner-up margin, then self-join provisionally committed group memberships to detect all conflicts."
    },
    {
      "level": 3,
      "kind": "boundary",
      "text": "Two indistinguishable references competing for one partner are both ambiguous; do not greedily let the first win."
    }
  ],
  "output": {
    "columns": [
      {
        "name": "ref_id",
        "type": "BIGINT",
        "nullable": false
      },
      {
        "name": "group_key",
        "type": "VARCHAR",
        "nullable": true
      },
      {
        "name": "confidence",
        "type": "VARCHAR",
        "nullable": false
      },
      {
        "name": "score",
        "type": "BIGINT",
        "nullable": false
      },
      {
        "name": "margin",
        "type": "BIGINT",
        "nullable": false
      }
    ],
    "ordering": [
      {
        "column": "ref_id",
        "direction": "ASC",
        "nulls": "LAST"
      }
    ]
  },
  "reference": "/bundle/challenges/reconcile.04/reference.sql",
  "expected": {
    "boundary-v1": "/bundle/challenges/reconcile.04/expected-boundary.json",
    "seed-20240907": "/bundle/challenges/reconcile.04/expected-seed-20240907.json",
    "seed-20240908": "/bundle/challenges/reconcile.04/expected-seed-20240908.json"
  },
  "assessment": {
    "kind": "exact"
  }
}
